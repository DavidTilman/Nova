<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Nova.Client.Pages.Accounts.AccountOverviewPageDefault"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nova.Client.Pages.Accounts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Border Margin="16" Background="{ThemeResource CardBackgroundFillColorDefault}" CornerRadius="{ThemeResource OverlayCornerRadius}">
        <Grid Margin="16" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Overview" Style="{ThemeResource TitleLargeTextBlockStyle}" Grid.ColumnSpan="2" HorizontalAlignment="Center"/>
            <TextBlock
                Grid.Row="1"
                Style="{ThemeResource TitleTextBlockStyle}"
                Text="Net Worth:"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"/>

            <TextBlock
                Grid.Row="1"
                Grid.Column="1"
                Style="{ThemeResource TitleTextBlockStyle}"
                Text="{x:Bind FormattedNetWorth}"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"/>

            <Border  Margin="0,16,0,0" Grid.Row="2" Grid.ColumnSpan="2" BorderBrush="{ThemeResource AccentAAFillColorDefaultBrush}" Height="1" BorderThickness="1"/>

            <ScrollView Grid.Row="3" Grid.ColumnSpan="2" Margin="0,8,0,0" VerticalAlignment="Top">
                <ListView x:Name="AccountEventsListView" SelectionMode="None"/>
            </ScrollView>
        </Grid>
    </Border>
</Page>
