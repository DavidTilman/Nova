<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Nova.Client.Pages.AddInterestForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nova.Client.Pages.Accounts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Border Background="{ThemeResource CardBackgroundFillColorDefault}" Width="500" VerticalAlignment="Top" CornerRadius="{ThemeResource OverlayCornerRadius}">
        <Grid Margin="16" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Add Interest:" VerticalAlignment="Center"/>
            <NumberBox Grid.Row="0" Grid.Column="1" x:Name="InterestNumberBox" PlaceholderText="Enter interest amount"/>
        </Grid>
    </Border>
</Page>
