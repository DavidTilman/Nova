<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Nova.Client.Forms.PaymentFormPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nova.Client.Forms"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource BackgroundBrushTransparent}">

    <Border VerticalAlignment="Center" HorizontalAlignment="Center" Background="{StaticResource CardBackgoundBrush}" CornerRadius="{ThemeResource OverlayCornerRadius}" Padding="16" Width="auto">
        <Grid ColumnSpacing="8" RowSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>

            <Button Content="Back" Grid.Column="1" Click="BackButton_Click"/>
            <TextBlock Text="Make Payment" Grid.Column="0" Style="{ThemeResource SubtitleTextBlockStyle}" Foreground="{StaticResource ForegroundBrush}"/>

            <StackPanel Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Stretch" Spacing="8">
                <ComboBox x:Name="AccountsCombobox" PlaceholderText="Account" HorizontalAlignment="Stretch" SelectionChanged="AccountsCombobox_SelectionChanged"/>
                <NumberBox x:Name="AmountNumberBox" PlaceholderText="Amount"/>
                <AutoSuggestBox x:Name="PayeeAutoSuggestBox" PlaceholderText="Payee" HorizontalAlignment="Stretch" TextChanged="PayeeAutoSuggestBox_TextChanged" SuggestionChosen="PayeeAutoSuggestBox_SuggestionChosen"/>
                <DatePicker x:Name="PaymentDatePicker" HorizontalAlignment="Stretch"/>
                <Button Content="Submit Payment" HorizontalAlignment="Stretch" Click="SubmitPaymentButton_Click" Style="{ThemeResource AccentButtonStyle}" Background="{StaticResource AccentBrush}"/>
            </StackPanel>

        </Grid>
    </Border>
</Page>