<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Nova.Client.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nova.Client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Nova.Client.Controls"
    mc:Ignorable="d"
    Background="{StaticResource BackgroundBrush}">

    <Grid ColumnSpacing="8" RowSpacing="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <controls:NetWorthPanel Grid.Column="0" Grid.ColumnSpan="2"/>
        <controls:WealthDistributionPanel x:Name="WealthDistributionPanel" Grid.Column="3"/>

        <controls:AccountsPanel Grid.Row="1" Grid.Column="0"/>

        <controls:InvestmentPanel x:Name="InvestmentPanel" Grid.Row="1" Grid.Column="1"/>
        <controls:RecentActivityPanel Grid.Row="1" Grid.Column="2"/>
    </Grid>
</Page>