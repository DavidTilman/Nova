<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Nova.Client.Controls.TimeChangeTab"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nova.Client.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border CornerRadius="{ThemeResource ControlCornerRadius}">
        <Grid ColumnSpacing="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Foreground="{StaticResource ForegroundBrush}" Text="{x:Bind Indicator}"/>
            <TextBlock x:Name="ChangeTextBlock" Grid.Column="1" Style="{ThemeResource BodyStrongTextBlockStyle}" Foreground="{StaticResource ForegroundBrush}" Text="{x:Bind Change}"/>
        </Grid>
    </Border>
</UserControl>