<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Nova.Client.Controls.AccountPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nova.Client.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="8" ColumnSpacing="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <!-- Left Content (Name, Provider, Type) -->
        <TextBlock Grid.Row="0" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{StaticResource ForegroundDisabledBrush}" HorizontalAlignment="Left">
            <Run Text="{x:Bind Account.ID}" />
            <Run Text=" • " />  
            <Run Text="{x:Bind Account.AccountProvider}" />
            <Run Text=" • " />
            <Run Text="{x:Bind Account.AccountType}"/>
        </TextBlock>

        <TextBlock Grid.Row="1" Text="{x:Bind Account.AccountName}" Style="{StaticResource BodyTextBlockStyle}" Foreground="{StaticResource ForegroundBrush}" HorizontalAlignment="Left"/>

        <!-- Right Content (Balance, Last Update) -->
        <TextBlock Grid.Column="3" Grid.Row="0" x:Name="LastUpdateTextBlock" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Right" HorizontalTextAlignment="Right" Foreground="{StaticResource ForegroundDisabledBrush}"/>
        <TextBlock Grid.Column="3" Grid.Row="1" Text="{x:Bind Account.FormattedBalance}" Style="{StaticResource BodyStrongTextBlockStyle}" HorizontalAlignment="Right" HorizontalTextAlignment="Right" Foreground="{StaticResource ForegroundBrush}"/>
        <TextBlock VerticalAlignment="Bottom" Grid.Column="2" Grid.Row="1" x:Name="BalanceChangeTextBlock" Style="{ThemeResource CaptionTextBlockStyle}"/>
        <Grid Grid.Row="2" Grid.ColumnSpan="4" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <ProgressBar  Foreground="{StaticResource AccentBrush}" Minimum="0" Maximum="1" Value="{x:Bind Account.FractionOfNetWorth}"/>
            <TextBlock Grid.Column="1" Text="{x:Bind Account.FormattedPercentageOfNetWorth}" Style="{ThemeResource CaptionTextBlockStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource ForegroundDisabledBrush}"/>
        </Grid>
    </Grid>
</UserControl>