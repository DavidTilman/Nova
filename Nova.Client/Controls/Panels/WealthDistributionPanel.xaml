<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Nova.Client.Controls.WealthDistributionPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nova.Client.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:chart="using:Syncfusion.UI.Xaml.Charts">

    <Border Padding="16" CornerRadius="{ThemeResource OverlayCornerRadius}" Background="{StaticResource CardBackgoundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Distribution" Style="{ThemeResource TitleTextBlockStyle}" Foreground="{StaticResource ForegroundBrush}"/>
            <chart:SfCircularChart Grid.Row="1" x:Name="TypeBalancePieChart">
                <chart:SfCircularChart.Legend>
                    <chart:ChartLegend Placement="Bottom"/>
                </chart:SfCircularChart.Legend>
            </chart:SfCircularChart>
        </Grid>
    </Border>
</UserControl>