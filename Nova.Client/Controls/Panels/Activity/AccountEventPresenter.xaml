<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Nova.Client.Controls.AccountEventPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nova.Client.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Foreground="{StaticResource ForegroundDisabledBrush}" Style="{ThemeResource CaptionTextBlockStyle}">
            <Run Text="{x:Bind AccountEvent.AccountName}"/>
            <Run Text="•"/>
            <Run Text="{x:Bind AccountEvent.FormattedNewBalance}"/>
        </TextBlock>
        <TextBlock Grid.Column="0" Grid.Row="1">
            <Run Text="{x:Bind AccountEvent.EventType}"/>
            <Run Text=" • " x:Name="SeperatorRun"/>
            <Run Text="{x:Bind AccountEvent.SecondaryAccountName}"/>
        </TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="1" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{StaticResource ForegroundDisabledBrush}" Text="{x:Bind AccountEvent.TimeStamp.ToShortDateString()}"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind AccountEvent.FormattedValue}" TextAlignment="Right"/>
    </Grid>
</UserControl>