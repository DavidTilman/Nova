<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Nova.Client.Controls.InvestmentPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nova.Client.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{x:Bind Position.Ticker}" Foreground="{StaticResource ForegroundDisabledBrush}" Style="{ThemeResource CaptionTextBlockStyle}"/>
        <TextBlock Grid.Row="1" Text="{x:Bind Position.Name}" Foreground="{StaticResource ForegroundBrush}" Style="{ThemeResource BodyTextBlockStyle}"/>

        <TextBlock Grid.Row="1" Grid.Column="2" x:Name="GainTextBlock" Style="{ThemeResource BodyStrongTextBlockStyle}" Foreground="{StaticResource ForegroundBrush}"/>
    </Grid>
</UserControl>