<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Nova.Client.Controls.InvestmentPositionCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nova.Client.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{x:Bind Ticker}" Grid.Column="0"/>
            <TextBlock Text="{x:Bind CurrentPrice}" Grid.Column="1"/>
            <TextBlock Text="{x:Bind AveragePrice}" Grid.Column="2"/>
            <TextBlock Text="{x:Bind Quantity}" Grid.Column="3"/>
            <TextBlock x:Name="GainTextBlock" Text="{x:Bind Gain}" Grid.Column="4"/>
        </Grid>
    </Border>
</UserControl>
