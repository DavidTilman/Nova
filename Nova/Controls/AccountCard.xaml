<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Nova.Controls.AccountCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nova.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border Background="{ThemeResource CardBackgroundFillColorDefault}" CornerRadius="10" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Style="{ThemeResource SubtitleTextBlockStyle}" Text="{x:Bind Account.AccountName}"/>
            <TextBlock Grid.Row="1" Style="{ThemeResource SubtitleTextBlockStyle}" Text="{x:Bind Account.BalanceString}"/>
            <StackPanel Orientation="Horizontal" Grid.Row="2" Spacing="10">
                <TextBlock Text="{x:Bind Account.AccountProvider}" Style="{ThemeResource CaptionTextBlockStyle}"/>
                <TextBlock Text="{x:Bind Account.AccountType}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
